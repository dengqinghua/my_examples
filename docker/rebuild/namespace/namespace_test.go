package namespace

import (
	. "github.com/smartystreets/goconvey/convey"
)

func TestUTSNamespace(t *testing.T) {
	Convey("Another space", t, func() {
		Convey("Should have seperate process", func() {
		})
	})
}
